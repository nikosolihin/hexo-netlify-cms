include icon

header.container
  a.logo(href=config.root)
    +icon('logo')

  nav.menu(role='navigation')
    each url, label in theme.menu
      a.menu__link.bold(
        href=url
        class=(is_current(url) ? 'menu__link--active' : false)
      )= label